//
// Agent SDK Types — re-exports from @cabinet/types for third-party developers.
//
// Agent developers should NOT need to import from @cabinet/types directly;
// all required types are surfaced through this SDK.
//

export type {
  ContextSlot,
  ExternalAgentConfig,
  ExternalAgentProtocol,
  AgentConfigSource,
} from '@cabinet/types';
