{
  "name": "@cabinet/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cabinet/storage": "workspace:*"
  },
  "devDependencies": {
    "@cabinet/types": "workspace:*",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
