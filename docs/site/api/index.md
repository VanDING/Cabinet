# API Overview

Cabinet provides a REST API for all operations.

Base URL: `http://localhost:3000/api`

## Authentication

Include the API key in the `Authorization` header:

```
Authorization: Bearer your-api-key
```
