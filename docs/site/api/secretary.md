# Secretary API

The Secretary API provides natural language interaction with the Cabinet system.

## Endpoints

- `POST /api/secretary/chat` — Send a message and receive a response
- `GET /api/secretary/history` — Retrieve conversation history
