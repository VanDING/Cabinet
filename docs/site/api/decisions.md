# Decisions API

The Decisions API manages decision cards with L0-L3 classification.

## Endpoints

- `POST /api/decisions` — Create a new decision
- `GET /api/decisions` — List decisions
- `PUT /api/decisions/:id` — Update a decision
- `POST /api/decisions/:id/vote` — Cast a vote
