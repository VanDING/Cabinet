# Deployment

## Docker

```bash
docker compose up -d
```

## Manual

```bash
cd apps/server && pnpm build && pnpm start
```

See the `docs/signing-guide.md` in the repository for desktop app distribution.
